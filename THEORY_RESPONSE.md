# Теоретическое задание: параллелизуемость/code divergence/memory coalesced access

Роман Чечеватов, ИТМО

---

> **1)** Пусть на вход дан сигнал x[n], а на выход нужно дать два сигнала y1[n] и y2[n]:
> 
> ```
>  y1[n] = x[n - 1] + x[n] + x[n + 1]
>  y2[n] = y2[n - 2] + y2[n - 1] + x[n]
> ```
> 
> Какой из двух сигналов будет проще и быстрее реализовать в модели массового параллелизма на GPU и почему?

`y1` реализовать проще.
У `y2` каждое следующее вычисление зависит от результата двух предыдущих, а потому вычислять параллельно -- нетривиально.
`y1` же прочитали несколько чисел, так они еще и хорошо переиспользуются в рамках варпа -- красота

---

> **2)** Предположим что размер warp/wavefront равен 32 и рабочая группа делится
> на warp/wavefront-ы таким образом что внутри warp/wavefront
> номер WorkItem по оси x меняется чаще всего, затем по оси y и затем по оси z.
> 
> Напоминание: инструкция исполняется (пусть и отмаскированно) в каждом потоке warp/wavefront если хотя бы один поток выполняет эту инструкцию неотмаскированно. Если не все потоки выполняют эту инструкцию неотмаскированно - происходит т.н. code divergence.
> 
> Пусть размер рабочей группы (32, 32, 1)
> 
> ```
> int idx = get_local_id(1) + get_local_size(1) * get_local_id(0);
> if (idx % 32 < 16)
>     foo();
> else
>     bar();
> ```
> 
> Произойдет ли code divergence? Почему?

int idx = y + height * x

Внутри варпа x меняется чаще всего, а так как размер рабочей группы по иксу равен 32 равен размеру варпа, 
то у всех лилипутов в варпе разные иксы и одинаковый игрек. y + 32 * x_1 mod 32 = y mod 32 -- константа в варпе.
Все лилипуты в варпе пойдут по одному пути -> **без code divergence**

---

> **3)** Как и в прошлом задании предположим что размер warp/wavefront равен 32 и рабочая группа делится
> на warp/wavefront-ы таким образом что внутри warp/wavefront
> номер WorkItem по оси x меняется чаще всего, затем по оси y и затем по оси z.
> 
> Пусть размер рабочей группы (32, 32, 1).
> Пусть data - указатель на массив float-данных в глобальной видеопамяти идеально выравненный (выравнен по 128 байтам, т.е. data % 128 == 0). И пусть размер кеш линии - 128 байт.

Получаем y = const в варпе

> (a)
> ```
> data[get_local_id(0) + get_local_size(0) * get_local_id(1)] = 1.0f;
> ```
> 
> Будет ли данное обращение к памяти coalesced? Сколько кеш линий записей произойдет в одной рабочей группе?

x + width * y

coalesced, игрек константа меняется медленно, а вот иксы подряд идут, индексы подряд идут

Записываем 32 float-а по 4 байта подряд, выровненно -> 1 кеш-линия

> (b)
> ```
> data[get_local_id(1) + get_local_size(1) * get_local_id(0)] = 1.0f;
> ```
> 
> Будет ли данное обращение к памяти coalesced? Сколько кеш линий записей произойдет в одной рабочей группе?

y + height * x

Не-coalesced, индекс меняется сразу на 32, сразу на 128 байт. Каждый лилипут свою кеш-линию -> 128 линий 

> (c)
> ```
> data[1 + get_local_id(0) + get_local_size(0) * get_local_id(1)] = 1.0f;
> ```
> 
> Будет ли данное обращение к памяти coalesced? Сколько кеш линий записей произойдет в одной рабочей группе?

1 + x + width * y

Обращения собраны подряд, но последний лилипут в варпе не входит в кеш-линию со своими друзьями и попросит себе
отдельную кеш-линию. 2 кеш-линии

Прямо строго определение coalesced не запомнил, и считается ли вообще это качественным свойством или количественным?..

Это более coalesced чем (b), но менее чем (a) -- второе обращение к памяти таки нужно.